# Browser options: chrome | firefox
BROWSER=chrome

# Force headless mode
HEADLESS=true

# Base URL for tests
BASE_URL=https://www.saucedemo.com

# Default explicit wait timeout (seconds)
DEFAULT_TIMEOUT=10
